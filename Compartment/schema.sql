/* (Beta) Export of data model Compartment of the subject dataModel.Agrifood for a postgreSQL database. Pending translation of enumerations and multityped attributes */
CREATE TYPE sex_type AS ENUM ('M', 'F', 'unknown', '');CREATE TYPE Compartment_type AS ENUM ('Compartment');
CREATE TABLE Compartment (additionalInfo json, address json, alternateName text, areaServed text, arrivalTimestamp timestamp, avgGrowth text, avgWeight text, buildingId text, co2 text, companyId text, dataProvider text, dateCreated timestamp, dateModified timestamp, description text, empty text, farmId text, feedConsumption text, humidity text, id text, lastUpdate text, location json, luminosity text, name text, numAnimals text, owner json, parentCompartmentId text, relatedSource json, seeAlso json, sex sex_type, source text, temperature text, type Compartment_type, waterConsumption text, weightStDev text);